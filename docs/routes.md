# The Circlebook — Complete Route Taxonomy & Sub-Views Map

## Master Taxonomy Map

1. **🚀 ENTRY** (`#entry`)
   - Welcome Screen (`#entry/welcome`)
   - Splash / Loading Screen (`#entry/splash`)
   - Onboarding Steps 1-3 (`#entry/onboarding1`, `#entry/onboarding2`, `#entry/onboarding3`)

2. **🔐 AUTH** (`#auth`)
   - Login (`#auth/login`)
   - Register (`#auth/register`)
   - Forgot Password (`#auth/forgot`)

3. **🏠 HOME** (`#home`)
   - Feed (`#home/feed`)
   - Trending (`#home/trending`)
   - Recommended (`#home/recommended`)
   - What's Happening (`#home/happening`)

4. **🔎 DISCOVER** (`#discover`)
   - Home (`#discover/home`)
   - People (`#discover/people`)
   - Smart Search (`#discover/smart_search`)
   - Communities (`#discover/communities`)
   - Events (`#discover/events`)
   - Opportunities (`#discover/opportunities`)

5. **👥 MY CIRCLE** (`#mycircle`)
   - All Connections (`#mycircle/all`)
   - Inner Circle (`#mycircle/inner`)
   - Friends (`#mycircle/friends`)
   - Work (`#mycircle/work`)
   - Requests (`#mycircle/requests`)

6. **👤 PROFILE** (`#profile`)
   - Overview (`#profile/overview`)
   - Posts (`#profile/posts`)
   - About (`#profile/about`)
   - Skills (`#profile/skills`)
   - Career Profile (`#profile/career`)

7. **💬 COMMUNICATION** (`#communication`)
   - Chat (`#communication/chat`)
   - Group Chat (`#communication/group`)
   - Message Requests (`#communication/requests`)

8. **🔔 NOTIFICATIONS** (`#notifications`)
   - All (`#notifications/all`)
   - Social (`#notifications/social`)
   - Messages (`#notifications/messages`)
   - Opportunities (`#notifications/opportunities`)

9. **🌐 COMMUNITIES** (`#communities`)
   - Discover (`#communities/discover`)
   - Details (`#communities/details`)

10. **📅 EVENTS** (`#events`)
    - Discover (`#events/discover`)
    - My Events (`#events/my_events`)

11. **💼 CAREER HUB** (`#career`)
    - Dashboard (`#career/dashboard`)
    - Jobs (`#career/jobs`)
    - Recommendations (`#career/recommendations`)

12. **🤖 CIRCLE AI** (`#ai`)
    - AI Home (`#ai/home`)
    - People Finder (`#ai/people_finder`)
    - Post Assistant (`#ai/post_assistant`)

13. **⚙️ SETTINGS** (`#settings`)
    - Appearance (`#settings/appearance`)
    - Privacy (`#settings/privacy`)
    - Security (`#settings/security`)

14. **🛡️ SAFETY** (`#safety`)
    - Safety Center (`#safety/center`)
    - Report Center (`#safety/report`)

15. **🧑💼 ADMIN** (`#admin`)
    - Admin Dashboard (`#admin/dashboard`)
    - User Management (`#admin/users`)
    - Moderation Queue (`#admin/reports`)
