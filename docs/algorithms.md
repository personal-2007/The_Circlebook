# Connection & Recommendation Algorithms

## Smart Circle Matching Algorithm

```javascript
score = (sharedInterests * 25) + (sharedSkills * 20) + (mutualFriends * 15) + (sameCollege ? 10 : 0);
```

The algorithm computes mutual affinity vectors to display match percentages and common connection reasons in Smart Search.
