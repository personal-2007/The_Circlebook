/* =========================================================
   THE CIRCLEBOOK - PEOPLE DIRECTORY CONTROLLER
   ========================================================= */

const CirclebookPeople = {
    search(query) {
        CirclebookApp.filterDiscoverPeople(query);
    }
};
