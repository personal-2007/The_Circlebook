/* =========================================================
   THE CIRCLEBOOK - DISCOVER MODULE
   ========================================================= */

const CirclebookDiscover = {
    exploreTab(tab) {
        CirclebookRouter.navigate("discover", tab);
    }
};
