/* =========================================================
   THE CIRCLEBOOK - CIRCLE AI CONTROLLER
   ========================================================= */

const CircleAI = {
    ask(prompt) {
        CirclebookApp.triggerAIPrompt(prompt);
    }
};
